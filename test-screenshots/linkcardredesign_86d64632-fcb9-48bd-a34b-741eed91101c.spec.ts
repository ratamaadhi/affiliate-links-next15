import { test } from '@playwright/test';
import { expect } from '@playwright/test';

test('LinkCardRedesign_2025-10-08', async ({ page, context }) => {
  // Navigate to URL
  await page.goto('http://localhost:3001');

  // Navigate to URL
  await page.goto('http://localhost:3001');
});
